//익명함수 
//클로저 

1. 퀴즈 
2. 익명함수
3. 클로저
4. 어제 내준 숙제 리뷰 

1. 조건문 종류 3가지 
let a =10;
let b =20;

a > b console.log(`${a}는 b보다 큽니다`);
console.log(`${b}가 a보다 큽니다. `);

let animal = "dog"

switch 문을 이용해서 animal이라는 변수가 'dog인경우 animal 변수는 dog입니다.'
bird인경우 'anmial 변수는 bird입니다.'

for문이랑 while문은 어떠한 차이가 있나요 

for문을 이용해서 const array_1 = [];

0부터 25까지; 

array_1의 버킷을 순차적으로 출력하는 for문을 만들어보세요

함수는 왜 사용할까요 ? //코드 재사용 목적 
함수란 뭐죠 ? //하나의 코드의 집합 

fucntion dummyFunc(param){

}

//parameter의 역할 argument로 들어온 값이 파라메터로 매핑되어 변수로써 사용가능한 
//가상의 변수 

dummyFunc(args);

//param와 매핑하기 위한 실제 값

return 키워드 의미 ? return 키워드를 만나면 함수는 내부에서 반환 되는 값이 된다. 
// 함수가 return키워드를 만나는 즉시 종료된다. 

makeRandomArray함수를 만드는데, 랜덤한 길이의 랜덤한 값이 들어가있는 배열을 반환 하는 함수를 만드세요. 

// printArray(배열) 배열의 처음 부터 끝까지 출력하는 함수를 만드세요

2번쨰 param를 asc라고 주고 function printArray(arr,asc){

}

//만약에 asc 가 1이라면 처음부터 끝까지 버킷을 출력하면 되구요. 
//만약에 asc 가 0 이라면 반대로 끝방부터 처음까지 버킷을 출력하면 됩니다. 

//전역변수 
//지역변수 

//함수표현식을 이용해서 sum함수를 만들 것이다. 
//2개의 전달인자를 받아 두 값을 합친 결과를 반환하는 함수 

//함수 표현식을 이용해서 oddEvenPrint함수를 만들것이다. 
//전달인자로 받은 숫자가 짝수라면 `{${값} 은 짝수입니다.}` || `${값}은 홀수입니다. `;

let global = 20;

function dummyFunc(){

}
dummyFunc()
cosole.log(global)

//let 으로 dog이라는 변수 선언
//const 으로 animal 이라는 상수 선언
//dog변수를 '치와와'로 재할당 
//const arr_1 = [];
//5번 버킷에 10 
//5번 버킷에 있는 값에다가 20을 더하세요 

//arr_1[5] += 20;

//10번 버킷에 arr_1[5] 과 20 을 더한 값을 할당하세요 

//함수를 이용해서 전역변수 arr_1의 1번 버킷에 값을 30으로 할당하세요 

//빈배열 전달인자로 받아 /뒤에서부터 10번째 방부터 0번째 방까지 /10 9 8 7 빈배열에 값을 
//거꾸로 할당하는 함수를 만들어보세요 